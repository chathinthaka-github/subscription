{
    "name": "subscription-system/backend",
    "description": "Pure PHP subscription management system with RabbitMQ workers",
    "type": "project",
    "require": {
        "php": ">=7.4",
        "php-amqplib/php-amqplib": "^3.0"
    },
    "require-dev": {
        "phpunit/phpunit": "^9.5"
    },
    "autoload": {
        "psr-4": {
            "": "src/"
        }
    },
    "scripts": {
        "test": "vendor/bin/phpunit"
    },
    "config": {
        "optimize-autoloader": true
    }
}

